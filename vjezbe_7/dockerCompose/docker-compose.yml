
services:
  auth-api:
    image: auth-api:1.0
    ports:
      - "9000:9000" # čitamo varijablu okruženja PORT iz .env datoteke
    env_file:
      - .env # učitavamo varijable okruženja iz .env datoteke
    networks:
      - interna_mreza

  social-api:
    image: social-api:1.0
    ports:
      - "3500:3500"
    networks:
      - interna_mreza

networks:
  interna_mreza:
    driver: bridge
